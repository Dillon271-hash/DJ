# Copy this file to .env and fill in real values from
# https://developer.spotify.com/dashboard (create an app there, then
# open its Settings to reveal these). Used server-side only — never
# exposed to the browser. Also add the same two as Environment
# Variables in your Vercel project settings for the deployed version.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
